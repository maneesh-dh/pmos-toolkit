# Simulation Trace — /plan skill redesign

**Date:** 2026-05-08
**Spec:** `../02_spec.md` (Tier 2)
**Scenarios traced:** 32
**Gaps surfaced:** 44 (1 blocker, 16 significant, 27 minor)
**Spec patches applied:** 40
**Open Questions deferred:** 2 (already in spec §11)

---

## 1. Scope

**In scope:** /plan SKILL.md body redesign; plan output document structure (frontmatter + body + sidecars `03_plan_review.md`, `03_plan_auto.md`, `03_plan_defect_*.md`); AskUserQuestion shapes; cross-skill *contracts* (frontmatter for /spec, new task fields for /execute, type field for /backlog, `_shared/stacks/<stack>.md` library, `_shared/platform-strings.md`).

**Out of scope (per spec §3 Non-Goals):** /spec v_next internals, /execute v_next internals, /backlog internals, pre-v2 plan migration, i18n, multiple plan-doc files per feature, no-spec entry handling beyond refusal, per-task effort estimates.

**Companion specs:** none yet — three are *implied* (/spec v_next, /execute v_next, /backlog type field). Tracked as cross-skill changes in spec §7 contracts.

**Anticipated consumers:** /execute v_next (forward-compat critical), /verify (broken-ref re-check), autonomous invocations (subagent / scheduled / CI).

## 2. Scenario Inventory

32 scenarios across 4 sources. Full list in chat trace; summary distribution:

| Source | Count |
|--------|-------|
| Spec (Journeys + Edge Cases) | 16 (S1-S16) |
| Variants | 3 (S17-S19) |
| Adversarial | 5 (S20-S24) |
| Model-driven | 8 (S25-S32) |

Categories: happy (3), edge (10), concurrency (3), retry (5), failure (5), timing (2), input (4), ordering (1), boundary (0), auth (0).

## 3. Coverage Matrix

Compressed: only scenarios that surfaced gaps. Happy paths (S1, S2, S3) traced cleanly except where noted. Detail in Gap Register.

S1 (Tier-1 backlog bug fix): hit S1-1 (T0 vs reduced TN) — patched via FR-12a.
S2 (Tier-3 wireframed Node multi-phase): hit S2-1 (phase-boundary verify) — patched via FR-26a.
S3 (mid-execute defect → fix-from): hit S3-1 (upstream root cause) — patched via FR-67a.
S4-S16 (E1-E13): clean traces against spec edge cases.
S17 (subagent-invoked autonomous): hit S17-1 (nested subagent) — patched via FR-42b.
S18 (Append mode): hit S18-1+S18-2 — patched via FR-60a.
S19 (two devs concurrent drafts): hit S19-1 (Draft excluded from in-flight) — patched via FR-54a clarification.
S20-S24 (adversarial): all surfaced gaps patched (S20-1 → FR-52a, S21-1 → FR-66, S22-1 → FR-31b, S23-1 → FR-42a, S24-1 → FR-50a).
S25-S26 (Open Q's): deferred to spec §11.
S27 (Bun/pnpm/yarn-berry): hit S27-1 — patched via FR-10/10a/11.
S28-S31 (sidecar fragility quartet): patched via FR-43a/b/c/d.
S32 (three-signal precedence): patched via FR-104a.

## 4. Artifact Fitness Findings

**Bucket 1 (sidecar persistent stores):** 5 gaps — B1-1, B1-2 (significant: structures undefined) → §7.6 added; B1-3 (BLOCKER: defect file structure) → §7.5 added; B1-4 (sidecar versioning) → FR-100a; B1-5 (lifecycle) → FR-100b.

**Bucket 2 (cross-skill service interfaces):** 4 minor gaps — B2-1 → FR-110; B2-2 → FR-111; B2-3 → FR-112; B2-4 → FR-113.

**Bucket 3 (review-loop state machine):** 2 gaps — B3-1 (significant: Cap-Hit Abandon behavior) → FR-67; B3-2 → FR-41b.

**Bucket 4 (interface):** N/A — covered in Phase 5.

**Bucket 6 (operational):** 2 minor gaps — B6-1 → FR-114 (NFR-N/A documented); B6-2 → FR-115 (rollout order).

## 5. Interface ↔ Core Cross-Reference

The "interface" is AskUserQuestion. Original spec §8 had 3 shapes (8.1-8.3). Reverse scan against every FR invoking AskUserQuestion found 4 missing shapes:

| # | FR/Edge | Shape Added |
|---|---|---|
| W1 | FR-51 (simulate-spec findings surface) | §8.6 |
| W2 | FR-65 (folder picker) | §8.5 |
| W3 | FR-14 (stack ambiguity) | §8.4 |
| W4 | E13 (spec re-open) | §8.7 |

All four added to spec.

## 6. Pseudocode (2 flows)

Full text of both flows in chat trace; key takeaways:

**Flow 1 — `review_loop`:** state machine `Loop_N → Loop_N+1 | Clean | Cap_Hit`; cap-hit branches to `Continue | Accept | Abandon | AutoAccept`. Concurrency notes flagged S21 (concurrent /plan instances). Error branches flagged S23 (subagent timeout) and S30 (silent sidecar write failure). Both patched.

**Flow 2 — `replan_overwrite`:** multi-file write across plan, archive, sidecar. No transactional rollback; concurrent Replan from two sessions = race on plan write order (S21). Same-day archive heading collision (S31). Both patched.

## 7. Gap Register

44 entries total. Severity distribution:

| Severity | Count | Of which patched | Deferred | Accepted-as-risk |
|----------|-------|------------------|----------|------------------|
| Blocker | 1 | 1 | 0 | 0 |
| Significant | 16 | 14 | 2 (Open Q #1, #2) | 0 |
| Minor | 27 | 27 | 0 | 0 |
| Forward-compat | 0 | — | — | — |

Full register kept in chat trace; spec §11 retains the 2 deferred.

## 8. Accepted Risks

None — no findings dispositioned as "Accept as risk." Every patchable gap was patched.

## 9. Open Questions

(Already tracked in spec §11; this section restates the 2 simulate-spec touched.)

| # | Question | Source | Owner | Needed By |
|---|----------|--------|-------|-----------|
| 1 | /spec anchor scheme: H2-derived auto vs explicit `{#anchor}` markers — affects FR-31 plumbing | Spec §11 / S25 | spec-author | before /spec v_next implementation |
| 2 | /execute v_next: cross-phase `**Requires state from:**` resolution behavior | Spec §11 / S26 | execute-author | before /execute v_next implementation |

## 10. Spec Patches Applied

40 patches, batched in 4 AskUserQuestion rounds. Summary of FR additions/modifications:

**Round 1 (4 patches — blocker + 3 significant):**
- §7.5 added: Defect file contract (B1-3 blocker)
- FR-31b added: Spec content drift detection (S22-1)
- FR-66 added: Concurrent-invocation lockfile (S21-1)
- FR-54a tightened: Draft included in in-flight (S19-1)

**Round 2 (4 significant patches):**
- FR-60a added: Append-mode ordering + dedupe scope (S18-1+2)
- FR-104a added: Three-signal precedence (S32-1)
- FR-10/10a/11 expanded: JS-stack disambiguation (S27-1)
- FR-61a added: Non-interactive halt protocol with exit code 2 (S16-1)

**Round 3 (4 consolidated patches):**
- §7.6 added: Sidecar contracts (B1-1, B1-2)
- §8.4-8.7 added: Missing AskUserQuestion shapes (W1-W4)
- FR-67/67a/67b added: Termination + --fix-from edge cases (B3-1, S3-1, S13-1)
- FR-26a added: Phase-boundary /verify trigger (S2-1)

**Round 4 (consolidated minors — 18+4 patches):**
- FR-43a/b/c/d added: Skip List integrity, atomic writes, archive uniqueness (S28-S31)
- §6.11 added: 18 minor-hardening FRs (FR-12a, FR-14a, FR-16, FR-16a, FR-40a, FR-42a, FR-42b, FR-50a, FR-52a, FR-100a, FR-100b, FR-110, FR-111, FR-112, FR-113, FR-41b, FR-114, FR-115)

Spec section count grew by 4 (§6.10, §6.11, §7.5, §7.6) plus 4 new AskUserQuestion shapes in §8. Original Platform Neutrality renumbered §6.10 → §6.12.

## 11. Review Log

| Loop | Findings | Changes Made |
|------|----------|-------------|
| 1 | Sweep against all 4 review checks (scenario completeness, bucket completeness, cross-reference completeness, gap-register integrity, high-severity coverage). All checks pass: 32 scenarios cover spec + variants + adversarial + model-driven; all 4 in-scope buckets critiqued; cross-reference both forward and reverse; every gap dispositioned; the 1 blocker has applied patch. | None — first loop is clean. Spec is ready for /plan. |

---

**Recommended next step:** `/pmos-toolkit:plan` (or `/grill 02_spec.md` for one more adversarial pass before planning).

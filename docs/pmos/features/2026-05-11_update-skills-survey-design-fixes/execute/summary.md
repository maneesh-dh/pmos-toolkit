# /execute log — survey-design hardening (03_plan.html)

Branch: `feat/survey-design-fixes`. Commit cadence: per-task. No worktree (already on the feature branch with the requirements/spec/plan commits).

## Tasks

| Task | Status | Commit | Notes |
|---|---|---|---|
| T0 | done | — | prereqs: on `feat/survey-design-fixes`, tree clean apart from feature folder, sources read. **DEVIATION:** the lint is at `plugins/pmos-toolkit/tools/audit-recommended.sh`, not repo-root `tools/` as the plan assumed. |
| T1 | done | `feat(survey-design,T1)` | `reference/survey-best-practices.md`: leading "Product fit (evaluate this first)" section (3 binary checks + survey-wide coverage + 2 worked examples), "Scoring rubric (0–100, informational)" section (8 dims summing to 100, deduction sizes, composite formula, "never gates the loop"), intro WIIFM requirement. |
| T2 | done | `feat(survey-design,T2)` | `SKILL.md`: Phase 4 rewritten as the bounded generate↔review loop (≤2 iters, categorical exit; §4.1 dispatch / §4.2 return contract incl. machine block / §4.3 parent side; reviewer evaluate-only, never mutates `survey.json`); Phase 5 opens with the changelog summary + orders product-fit FAILs first ("Kill or rewrite Q<id>?" with `Rewrite as proposed (Recommended)`); Anti-Patterns updated (bounded loop, reviewer-doesn't-mutate); Edge-cases E15–E19 added; reference-files description updated. `audit-recommended.sh` exit 0; non-interactive block byte-match clean. |
| T3 | done | `feat(survey-design,T3)` | `SKILL.md`: Phase 2 `response_impact` variable (free-form ask, `<!-- defer-only: free-form -->`); §3.1 `intro.response_impact` field + `schema_version` 1→2 + rewritten WIIFM `intro.text` example + v1→v2 migration note; Phase 3.4 WIIFM-line requirement (honesty constraint). |
| T4 | done | `feat(survey-design,T4)` | `SKILL.md`: Phases 3.6 & 7 two-tier substrate-asset handling — hard-required (`survey-preview.js`/`style.css`/`viewer.js`) → abort with `survey-design: missing substrate asset <abs-path> — reinstall the pmos-toolkit plugin`, no degraded artifact; convenience (`serve.js`; turndown trio / `html-to-md.js`) → one warning, skip, continue. Platform-Adaptation bullet + Edge-cases E20/E21. |
| T5 | done | `feat(survey-design,T5)` | `SKILL.md`: Phases 5 & 6 cosmetic-only carve-out (severity `nit` AND wording/title polish, no methodological/product-fit weight) → trailing "Noted, not asked:" line, still recorded in `## Dispositions` as `noted (cosmetic, not asked)`; Anti-Pattern note; Edge-case E22. (Initial commit tripped the lint on two `AskUserQuestion` mentions in non-call-site prose; reworded + `git commit --amend`; lint clean.) |
| T6 | done | `feat(survey-design,T6)` | `SKILL.md`: §3.2 `multi_field_open` type added to the `type` enum + `fields: [{id,label,placeholder}]` array in the question object; §3.3 `multi_field_open = 30 per field`; schema invariants (≥1 field, kebab-unique ids, no options/scale/rows/columns/constant_sum_total, not routable); §3.1 `schema_version: 2` confirmed + migration note already names both additions. |
| T7 | done | `feat(survey-design,T7)` | `reference/platform-export.md`: `multi_field_open` added to the enum line + an explainer note + a row in all 4 platform type-mapping tables — **SurveyMonkey native** (`open_ended`/`multi`, `answers.rows[]` = field labels — P4 determination: confirmed native against the existing doc's documented `open_ended`/`multi` family), **Qualtrics native** (`Matrix`/`TE`), **Typeform downgrade** (statement + N `short_text`), **Google Forms downgrade** (`addSectionHeaderItem` + N `addTextItem`); README-template note about the downgrade + in-artifact comment. |
| T8 | done | `feat(survey-design,T8)` | `assets/survey-preview.js`: `TYPES.MULTI_FIELD_OPEN`, `renderMultiFieldOpen(q)` (one `<label for>`+`<input type="text">` per field, label-adjacent + associated, placeholder from `field.placeholder`, opt-out rendered after `<hr>`), branch in `renderQuestion`; counted as one question (not `STATEMENT`). `node --check` passes. **Manual browser render check deferred to `/verify`** (no browser/jsdom in this environment). |
| T9 | done | `test(survey-design,T9)` | `tests/fixtures/multi-field-brief.txt` added (active-users-by-cadence brief that naturally reaches for `multi_field_open`); `tests/expected.yaml` `additional_scenarios:` block — `multi_field_open` (schema_version 2, type present, fields ≥1, invariants, 30s/field → 90s for 3 fields, one input per field, counted-as-one, SurveyMonkey-native / GForms+Typeform-downgrade, README lists downgrade) + `f8_intro_wiifm_regression` (same top-level keys + `intro.response_impact` + WIIFM sentence in `intro.text`, no new question types, reviewer flags missing WIIFM). YAML parses. |
| TN | done (static + structural); behavioral runbook → `/verify` | `test(survey-design,TN)` | **Static/structural — all pass:** `audit-recommended.sh` exit 0; non-interactive-block byte-match clean; Phase 4 = loop heading + reviewer-evaluate-only line; `schema_version: 2`; `multi_field_open` in SKILL.md / all 4 platform tables / `survey-preview.js` / `expected.yaml`; `survey-best-practices.md` leads with "Product fit (evaluate this first)" + has the 8-dim rubric (weights sum 100); `node --check` on `survey-preview.js` passes; `description` trigger-phrase line + `argument-hint` unchanged (no new flags). **Behavioral runbook (F1 reconstructed-retro-brief acceptance run, F3 rename-asset abort/warn checks, F6 export+render run, F7 cosmetic-nit check, F8 ask+WIIFM check, v1→v2 backward-compat, regression) not executed in /execute — these require running `/survey-design` end-to-end and are carried into the `/verify` stage (the pipeline's next step), which re-runs the multi-agent review + interactive QA on top of this.** |

## Final verification commands (re-runnable)

```
bash plugins/pmos-toolkit/tools/audit-recommended.sh plugins/pmos-toolkit/skills/survey-design/SKILL.md      # exit 0
diff <(sed -n '/non-interactive-block:start/,/non-interactive-block:end/p' plugins/pmos-toolkit/skills/survey-design/SKILL.md) \
     <(sed -n '/non-interactive-block:start/,/non-interactive-block:end/p' plugins/pmos-toolkit/skills/_shared/non-interactive.md)   # no diff
node --check plugins/pmos-toolkit/skills/survey-design/assets/survey-preview.js                               # OK
grep -F "multi_field_open" plugins/pmos-toolkit/skills/survey-design/reference/platform-export.md             # 4 platform-table rows + enum + note
```

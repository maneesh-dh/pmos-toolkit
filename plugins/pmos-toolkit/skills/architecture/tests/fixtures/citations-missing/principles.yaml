# citations-missing fixture (T19) — one rule lacking source:.
# check-citations.sh should exit 1 against this file with U001 surfaced as
# the offender. Used by tests/fixtures/citations-missing/.assert (T20).
schema_version: 1

rules:
  - id: U001
    tier: 1
    stack: null
    severity: warn
    delegate_to: grep
    check: "file_loc_gt:500"
    # source: deliberately missing for the negative test
    message: "No file > 500 LOC."
    why: "Files past 500 LOC are hard to load mentally."

  - id: U004
    tier: 1
    stack: null
    severity: warn
    delegate_to: grep
    check: "regex:console\\.log|print\\("
    source: "plugins/pmos-toolkit/skills/architecture/reference/l1-rationales.md#u004"
    message: "No console.log / print() in src/."
    why: "Stray print/log statements leak in prod."

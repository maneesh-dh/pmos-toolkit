// Purpose: T11 tool-missing fixture — mixed TS/Py tree; with both delegated tools stripped from PATH,
// audit must exit 0 with tools_skipped populated and only L1 findings emitted.
export const useless = 1;
